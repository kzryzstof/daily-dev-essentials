# AGENTS.md

Guidance for AI agents working in this repository. Read this first; pull in the
deeper reference docs (see [References](#references)) only when a task needs them.

## What this repo is

**Daily Dev Essentials** — a curated collection of code snippets, utilities,
IDE/tool configurations, and AI prompts (skills + references) that support the
Octelys development workflow. It is a grab-bag reference repo, **not a buildable
application**: there is no app to run and no test suite for the repo itself.

## Repository map

```
azure/        Azure Functions helpers (e.g. HostHelper.cs for DI in tests)
csharp/       Standalone C# snippets and test utilities (e.g. ObjectExtension.cs)
ide/          IDE config: Rider file-layout format, Copilot MCP server setup
prompts/      The AI assets — the part of the repo that is actively authored
├── skills/          One folder per skill, each with a SKILL.md
├── references/      Shared, in-depth guideline docs skills point to
├── configurations/  Reference configs for Claude Code / Codex + setup scripts
└── scripts/         update-skills.py — installs skills + references locally
```

## Skills and references — the model

Two layers, deliberately separated by **when they load**:

- **Skills** (`prompts/skills/<name>/SKILL.md`) — the *trigger + instructions*.
  Loaded on demand when the task matches the skill. Keep them focused on the
  skill's role, required inputs, workflow steps, and output format.
- **References** (`prompts/references/<name>.md`) — the *source of truth* for
  long, detailed rule sets (e.g. unit-test formatting, web-app structure). A
  skill should **point to** the relevant reference, not copy its content.

> **Single source of truth.** Do not duplicate a reference's rules inside a
> skill. If a skill needs those rules, have it read the reference file. This is
> what `update-skills.py` enables by installing `references/` alongside the skills.

## Authoring conventions

- **Frontmatter.** Every `SKILL.md` and reference starts with YAML frontmatter
  carrying `name:` and a `description:`. The description begins with a semantic
  version, e.g. `description: v1.7.0 - Applies the Octelys unit test ...`.
- **Versioning.** When you change a skill's or reference's behaviour, bump the
  version in the `description:` frontmatter **and** any traceability footer
  inside the body (e.g. `Generated by describe-pr skill v1.1.0`). Keep them in
  sync.
- **Naming.** Folder/file `name` is kebab-case and matches the frontmatter
  `name:`.
- **Cross-references.** When a skill relies on a reference, name it explicitly by
  path (`prompts/references/<name>.md`) so the installed copy resolves.

## Installing skills locally

`prompts/scripts/update-skills.py` copies the skills and `references/` into whichever
of `~/.claude`, `~/.copilot`, `~/.gemini`, `~/.codex`, `~/.junie` already exist.

```bash
python3 prompts/scripts/update-skills.py --dry-run          # preview only
python3 prompts/scripts/update-skills.py                    # install to all present
python3 prompts/scripts/update-skills.py --target .claude   # limit to one target
```

It only installs into target folders that already exist; missing ones are skipped.

## References

Pull these in when the task calls for them — they are long by design and are not
meant to be read on every task:

- `prompts/references/unit-test-guidelines.md` — Octelys C# unit-test formatting
  rulebook (source of truth for the `code-format-tests` skill).
- `prompts/references/code-web-app-conventions.md` — clean-architecture project
  structure for ASP.NET Core web hosts.
