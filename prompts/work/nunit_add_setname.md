Add the call to `SetName` to the existing `TestCaseData`. Use the following format:

`<Context>_<ExpectedValue>Returned`

The `Context` refers to the use case being tested, using the present tense.
The `ExpectedValue` refers the value being expected.

For example:

`CardNumberIsInvalid_InvalidDataFormatReturned`